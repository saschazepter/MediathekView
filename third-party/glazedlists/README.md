# Glazed Lists

The Glazed Lists Kotlin core source in `src/main/kotlin/ca/odell/glazedlists`
and resources in `src/main/resources/resources` are vendored from the upstream Glazed Lists
repository:

- Repository: https://github.com/glazedlists/glazedlists
- Revision: `9ace85251cc0f8e70eebc9dfdb2cb2a920cb0275`
- Revision date: 2025-05-11
- Upstream source path: `core/src/main`

Only the self-contained core module is included. Optional extension modules,
including all JavaFX, SWT, and Hibernate components, are intentionally excluded
because they are not used by MediathekView.

The following unused deprecated compatibility classes are also omitted in favor
of their retained replacements:

- `EventListModel` (`DefaultEventListModel`)
- `EventComboBoxModel` (`DefaultEventComboBoxModel`)
- `EventSelectionModel` (`DefaultEventSelectionModel`)
- `EventTableModel` (`DefaultEventTableModel`)

The obsolete Java 1.4 lock backport (`Lock`, `ReadWriteLock`, `LockFactory`, and
`J2SE50LockFactory`) is replaced by `java.util.concurrent.locks` types. The
default remains a non-fair `ReentrantReadWriteLock`.

Java object serialization support is intentionally omitted from the vendored
Glazed Lists types because MediathekView does not serialize those objects.

Unused internal `GlazedListsImpl` helpers for list concatenation, Latin
diacritics access, and legacy `Date`/`Calendar` month handling are omitted.

Upstream's `impl.testing` package is omitted from production sources. Its useful
event-consistency behaviors are covered by JUnit tests under `src/test`;
obsolete timing helpers and trivial test-data factories are not included.

## Kotlin conversions

Converted sources retain their upstream package and Java-facing contract unless
an intentional difference is documented below.

Top-level declarations in `ca.odell.glazedlists.impl` are Kotlin-internal when
they are not part of a retained public or protected source contract. Kotlin
`internal` leaves their JVM classes public for mixed Java/Kotlin linkage.
`ObservableConnector`, `BlockSequence`, `Tree4Deltas`, `SearchTerm`, `TextMatcher`,
`SortingState`, and `SortingStrategy` remain public in Kotlin because retained
Glazed Lists APIs expose them in type bounds, return types, protected members,
or parameters.

| Upstream Java source | Local source | Intentional differences |
| --- | --- | --- |
| `TransactionList.java` | `TransactionList.kt` | Adds `withTransaction`; rollback-disabled construction is internal. |
| `UndoRedoSupport.java` | `UndoRedoSupport.kt` | Uses Kotlin collection and functional-interface idioms. |
| `DisposableMap.java` | `DisposableMap.kt` | Preserves the Java `Map` and disposal API while adding Kotlin's mutable-map marker; Java implementations qualify `Map.Entry` to avoid marker ambiguity. |
| `Filterator.java` | `Filterator.kt` | Preserves Java SAM construction and the generic extraction method. |
| `TextFilterator.java` | `TextFilterator.kt` | Preserves Java SAM construction and the `BiConsumer` default bridge. |
| `TextFilterable.java` | `TextFilterable.kt` | Preserves Java SAM construction and the `Consumer` default bridge. |
| `ObservableElementChangeHandler.java` | `ObservableElementChangeHandler.kt` | Preserves Java SAM construction and the intentionally object-typed change callback. |
| `ObservableElementList.java` | `ObservableElementList.kt` | Preserves Java subclassing and constructor variance, object-typed element callbacks, identity-based listener tracking, shared-listener registry compaction and its irreversible per-element switch, exact source event forwarding, UNKNOWN old-value fallback, initialization rollback, retryable reentrant and concurrent disposal, source-lock release before connector callbacks, post-disposal silence, and runtime-visible nullable JSpecify connector metadata. Kotlin adds only private helper state and metadata; no public/protected Java API or bridge drift is accepted. |
| `ListSelection.java` | `ListSelection.kt` | Preserves all four selection modes, exact selected/deselected and toggling live views, Barcode identity state, source event/reorder projection, anchor/lead and listener ranges, matcher quirks, nullable raw-Java array failure timing, partial event transactions, stale post-disposal behavior, Java subclassing, and the Java-functional nested `Listener`. Private backing state keeps internal behavior independent of overridable JavaBean accessors. Accepted Kotlin ABI drift is limited to the public `Companion` field required by `const val` mode constants, synthetic `Consumer.accept` bridges, four primitive-only synthetic live-view mapping helpers, effectively private inner constructors, Kotlin nullability metadata, and the Kotlin source name `selectValue(E)` with `@JvmName("select")`; Java retains the open `select(Object)` descriptor and baseline-compiled Java and Kotlin binaries run unchanged. |
| `DebugList.java` | `DebugList.kt` | Preserves the open debugging hooks around every list operation, exact event forwarding, sanctioned-thread and lock diagnostics, reentrant acquisition bookkeeping, read-to-write upgrade rejection, shared-infrastructure factory behavior, JavaBean accessors, method-generic `createNewDebugList`, JSpecify array/function/collection metadata, and the intentionally non-idempotent disposal contract. Kotlin uses real non-null collection/function overrides rather than unsupported nullable pseudo-overrides, so raw Java nulls fail at the Kotlin entry boundary before hook dispatch instead of inside the delegate. Accepted private-only ABI drift comprises Kotlin `ListEventListener` consumer bridges, Kotlin metadata, final private lock helpers, and a public constructor inside the inaccessible private `ListEventForwarder`; no public/protected outer or lock API changed. |
| `SeparatorList.java` | `SeparatorList.kt` | Preserves grouping and separator insertion, sorted-source write-through, comparator replacement, minimum-size and per-group limit visibility, reorder projection and fallback replacement events, stale detached separators, exact disposal behavior, Java subclassing of the outer list and injector, the public static five-method `Separator` interface, and the historical Bug 499 transaction behavior. Kotlin has no package-private nested visibility, so the Kotlin-internal `SeparatorInjectorList` and `GroupSeparator` classes and their effective constructors are public in JVM bytecode; normal Kotlin callers still see them as internal. Their implementation state uses public synthetic JVM fields with Kotlin-internal source visibility to preserve direct inner/outer access without synthetic `access$` methods; `@JvmSynthetic` keeps those fields unresolved by normal Java source. No outer or `Separator` public/protected descriptor, generic signature, or overridability changed. |
| `EventList.java` | `EventList.kt` | Preserves the Java `List` and `AutoCloseable` interfaces, contravariant listener registration, lock and publisher accessors, disposal contract, and default `close()` delegation; Kotlin adds its standard mutable-list marker and default-interface compatibility bridge. |
| `TransformedList.java` | `TransformedList.kt` | Preserves the protected mutable source field (nullable for `PluggableList` disposal), Java subclassing, source-index translation, exact write and bounds diagnostics, shared lock/publisher infrastructure, listener disposal, and open mapped `size()` and indexed-removal descriptors; Kotlin adds standard consumer bridges for `ListEventListener`. |
| `RangeList.java` | `RangeList.kt` | Preserves Java construction and subclassing, head/middle/tail range semantics, source-index mutation mapping, incremental range events, open bound methods for `ThresholdList`, and final adjustment/listener/size hooks. |
| `BasicEventList.java` | `BasicEventList.kt` | Preserves all Java constructors, lock/publisher fallback behavior, random-access marker, mutation event batching and identity-aware replacement, traversal delegation, no-op disposal, and exact mapped collection descriptors. |
| `SortedList.java` | `SortedList.kt` | Preserves natural/custom/null comparator construction, strict and avoid-moving update modes, stable duplicate ordering, two-tree source/reorder translation, writable source-index mapping, exact sort-location searches, fast mutable iteration, and Java static factory/constant compatibility. |
| `UniqueList.java` | `UniqueList.kt` | Preserves comparator-defined uniqueness, duplicate counts and snapshots, writable representative mutation, comparator replacement, grouped event translation, sorted-source disposal, and the natural-order static factory. |
| `GroupingList.java` | `GroupingList.kt` | Preserves comparator-defined mutable group views, group lookup, group-tree index tracking, source write-through, whole-group replacement/removal, comparator rebuilding, disposal, and the natural-order static factory; Kotlin adds standard mapped-list bridges. |
| `FilterList.java` | `FilterList.kt` | Preserves static and editor-driven filtering, all matcher transition types, exact source event and reorder translation, writable filtered-index mapping, lock-aware initialization cleanup, listener replacement/disposal, and Java constructor variance. |
| `SequenceList.java` | `SequenceList.kt` | Preserves both Java constructors, nested `Sequencer` ABI and nullable implementation contract, natural/custom ordering, read-only random-access behavior, incremental sequence events, disposal behavior, and exact mapped list descriptors. |
| `ThresholdList.java` | `ThresholdList.kt` | Preserves Java construction, inclusive threshold and duplicate-boundary semantics, sorted writable range behavior, nested evaluator bridging, comparator value semantics, and sorted-source disposal. |
| `CompositeList.java` | `CompositeList.kt` | Preserves Java construction, CompositeList-specific extensibility, shared publisher/lock validation, identity-based member removal, and the method-generic member-list factory; Kotlin adds its standard mapped-collection bridges. |
| `FreezableList.java` | `FreezableList.kt` | Preserves snapshot isolation while frozen, thaw event reconstruction, source reconnection, write protection, lifecycle diagnostics, Java construction, and the `size()` descriptor; Kotlin adds its standard mapped-collection removal bridges. |
| `PluggableList.java` | `PluggableList.kt` | Preserves both Java constructors, replaceable-source event rebuilding, infrastructure validation, write-through behavior, disposal diagnostics, and Java subclassing, including open `size()` and indexed-removal descriptors. |
| `PopularityList.java` | — | Removed because the frequency-ranked list had no production consumers or subclasses. |
| `Sequencers.java` | `Sequencers.kt` | Preserves the Java-static month factory, construction-time time zone capture, month-boundary and overlap handling, null diagnostics, and non-instantiable utility-class contract. |
| `SyncListener.java` | `SyncListener.kt` | Preserves target drift detection, incremental event translation, idempotent disposal, and Java subclassing. |
| `event/ListEventListener.java` | `event/ListEventListener.kt` | Preserves Java SAM construction, `EventListener`, and the `Consumer` default bridge. |
| `event/ListEventPublisher.java` | `event/ListEventPublisher.kt` | Preserves Java implementation and nullable object-identity dependency parameters. |
| `impl/adt/barcode2/Element.java` | `impl/adt/barcode2/Element.kt` | Preserves Java constants and node methods while exposing color and sorted state as Kotlin properties. |
| `impl/adt/barcode2/ListToByteCoder.java` | `impl/adt/barcode2/ListToByteCoder.kt` | Rejects duplicate colors, documents the seven-bit capacity, omits unused decoding methods, and keeps `colorAsIndex` Kotlin-internal with a public JVM static bridge for Java callers. |
| `impl/adt/barcode2/SimpleNode.java` | `impl/adt/barcode2/SimpleNode.kt` | Preserves node identity, subtree counts, parent/child links, sorted-state flags, nullable stored values, and diagnostic tree rendering while keeping the node Kotlin-internal and marking its JVM-only internal surface synthetic to approximate the former package-private boundary in mixed builds. |
| `impl/adt/barcode2/SimpleTreeIterator.java` | `impl/adt/barcode2/SimpleTreeIterator.kt` | Preserves before-first and after-last cursor handling, start-index construction, copy semantics, node traversal, and failure modes. |
| `impl/adt/barcode2/SimpleTree.java` | `impl/adt/barcode2/SimpleTree.kt` | Preserves AVL rotations, indexed and sorted insertion, identity-sensitive node lookup, delete replacement paths, and comparator-driven behavior for current Kotlin and mixed Java/Kotlin callers; the Kotlin port also keeps invariant validation behind the class assertion status and removes the unreachable temporary split branch because `SimpleNode` width is always one. |
| `impl/adt/barcode2/FourColorNode.java` | `impl/adt/barcode2/FourColorNode.kt` | Preserves color-mask counts, block widths, parent/child links, nullable payload handling, diagnostic rendering, and node identity while keeping the node Kotlin-internal and its mutable JVM-only surface synthetic for mixed builds. |
| `impl/adt/barcode2/FourColorTreeIterator.java` | `impl/adt/barcode2/FourColorTreeIterator.kt` | Preserves before-first and after-last cursor handling, color-aware traversal, copy semantics, node and element indices, and the current boundary failure modes. |
| `impl/adt/barcode2/FourColorTree.java` | `impl/adt/barcode2/FourColorTree.kt` | Preserves AVL rotations, indexed insertion/removal, block splitting, identity-sensitive merging, color transitions, rank/select conversion semantics, and current Tree4Deltas-oriented behavior while keeping invariant validation behind the class assertion status. |
| `impl/adt/BarcodeNode.java` | `impl/adt/BarcodeNode.kt` | Preserves run-length-compressed black-node storage, cached subtree sizes, parent/child identity, AVL rotations, rank/select mapping helpers, unlink/compression behavior, and diagnostic validation while keeping the node Kotlin-internal with a private constructor. |
| `impl/adt/BarcodeIterator.java` | `impl/adt/BarcodeIterator.kt` | Preserves before-first cursor state, white/black rank tracking, color-specific traversal, remove/set mutation semantics, trailing-whitespace behavior, and current boundary failures while retaining Kotlin source compatibility for existing property-style callers. |
| `impl/adt/Barcode.java` | `impl/adt/Barcode.kt` | Preserves the public two-color compressed-list API, BLACK/WHITE singleton identity, run-length mutation complexity, white/black rank-select conversions, trailing-whitespace behavior, and current failure modes for mixed Java/Kotlin callers. |
| `impl/adt/SparseList.java` and `impl/adt/SparseListNode.java` | `impl/adt/SparseList.kt` and `impl/adt/SparseListNode.kt` | Preserve the sparse null-compression list API, sparse null-gap/node caching semantics, AVL rotations and deletion replacement identity, trailing-null insertion semantics, and the current unvalidated boundary and iterator-surface behavior for existing callers. |
| `event/SequenceDependenciesEventPublisher.java` | `event/SequenceDependenciesEventPublisher.kt` | Preserves identity-based listener registration and dependency ordering, reentrant pending-event queue semantics, exact exception aggregation and cleanup timing, related-subject redirection, and the Java-usable nested `EventFormat` contract required by `ListEventAssembler.kt`. The Kotlin `internal` publisher is public at the JVM class/constructor level because Kotlin has no package-private top-level visibility; Kotlin callers still see it as internal. |
| `event/ListEventAssembler.java` | `event/ListEventAssembler.kt` | Preserves the public constructor, public mutation/listener API, protected state fields, exact event batching/reorder behavior, deferred Java-null failure ordering, Kotlin and Java caller compatibility, and the `createListEventPublisher()` static entry point. Accepted Kotlin ABI drift comprises the required public `Companion` field and instance factory plus `@JvmStatic` bridge, its private no-arg and public synthetic marker constructors, final instance methods on the final class, the public `ListEventAssemblerKt` facade with six public-static-final-synthetic extension accessors, public synthetic getter bridges for former package-private helper accessors (with erased synthetic-helper signatures), synthetic `access$...` bridges for the inner event format, and the effective-private final `ListEventFormat` having a public constructor. The port also uses reflection to construct the package-private `Tree4DeltasListEvent` and carries Kotlin nullability/metadata annotations; normal Java source cannot call the synthetic helpers. |
| `AbstractEventList.java` | `AbstractEventList.kt` | Preserves constructor publisher identity, deferred nullable lock state, listener registration order/duplicates/null diagnostics, default iterator and sublist behavior, search-direction quirks, bulk-operation batching and cleanup, array snapshot semantics, unsupported-mutation messages, and the Java/Kotlin subclass contracts for `size()` and `get(int)`. Accepted Kotlin ABI drift comprises private backing fields for `readWriteLock` and `publisher`, their protected synthetic setters, and a private synthetic generic bridge that lets the raw Java getters retain legacy null results while the normal Kotlin properties remain non-null. Java subclasses can still override both getters but can no longer directly assign the former protected fields; no in-repository Java subclass does so. Additional drift comprises a concrete `close()` bridge declared on the class, and the private static lambda helpers named `removeAll$lambda$0`, `retainAll$lambda$0`, and `clear$lambda$0`. The protected `updates` field remains directly Java-accessible, and listener registration alone is nullable at the Kotlin boundary to retain the longstanding JVM diagnostics. Kotlin’s mandatory entry checks on non-null `MutableList` override parameters are accepted for raw-Java null calls: `addAll`, `removeAll`, `retainAll`, `removeIf`, and `replaceAll` now fail before legacy size checks or event setup; this cannot be avoided without retaining a Java compatibility implementation or globally disabling Kotlin parameter assertions. |
| `impl/Diff.java` | `impl/Diff.kt` | Preserves the Myers O(ND) edit-script tie-break, minimum insert/delete behavior, comparator-as-equality semantics including deferred null-comparator failure, duplicate stability, optional equal-element updates, and exact emitted mutation order. The diagonal frontier uses direct array indexing, and each matching snake retains only its endpoint to avoid hash lookups and per-match path allocations. The unused debug-only character accessors were removed. `Diff` is now a non-instantiable internal Kotlin object called directly by the Kotlin `GlazedLists` facade. |
| `GlazedLists.java` | `GlazedLists.kt` | Preserves all 45 public-static factory methods, erased descriptors, generic signatures, varargs flags, singleton identities, nullable collection/publisher/lock behavior, comparator and callback failure timing, and Java generic inference and method references. Kotlin necessarily adds the public `Companion` field and nested companion class, moves the four private final singleton fields into a private `Singletons` object with its `INSTANCE`, and adds a JVM-synthetic Kotlin-only assignment bridge so list-valued maps expose mutable groups while accepting read-only Kotlin lists. A raw Java null vararg-array reference cannot be represented by Kotlin: `eventListOf((Object[]) null)` and its publisher/lock overload now fail at the generated non-null parameter check with `NullPointerException` before constructing or mutating a list, while ordinary non-null varargs remain unchanged. |
| `impl/Grouper.java` | `impl/Grouper.kt` | Preserves the two-pass grouping state machine, comparator identity rebuilding, barcode color identity and patterns, exact group callback ordering and flags including GLAZEDLISTS-599 and Bug500 handling, Java-functional `Client`, and the Java-visible getters, setter, constants, and constructor required by `SeparatorList.kt`. Private backing state keeps internal operations independent of overridable getters as in Java; null clients remain accepted with failure deferred until callback dispatch, and comparator rebuild failures leave the newly installed partially populated barcode. The nullable comparator is explicit in Kotlin, so `GroupingList` uses a non-null assertion only at the comparison point where the Java implementation previously dereferenced it. |
| `CollectionList.java` | `CollectionList.kt` | Preserves flattened ordinary and live child lists, exact parent/child event normalization and index mapping, writable `set`/`remove` bridges, publisher and lock validation, related-subject cleanup, disposal, Java subclassability, and the Java-functional nested `Model` with its default `Function.apply` bridge. Java `Model` implementations that return null still install the parent tree and black-barcode entries before failing, so callers that explicitly discard the pending event retain the Java implementation's recoverable state. `Model.getChildren` accepts a nullable parent in Kotlin so raw Java null calls retain the platform-type behavior; existing Kotlin implementations and lambdas handle that nullable value explicitly. Private child wrappers delegate mutable outer state through JVM-synthetic internal helpers, keeping the helpers hidden from normal Java source while avoiding synthetic accessors that expose the barcode or tree. `CompositeList` explicitly retains the Java-visible `removeAt(int)` alias that its Kotlin compiler bridge previously emitted while extending the Java implementation; the replacement method is no longer marked `ACC_BRIDGE`, but preserves Java source and binary linkage. Raw Java `listChanged(null)` calls are intentionally rejected by Kotlin before an event transaction is opened rather than leaving the assembler in the Java implementation's corrupted open-event state. |
| `FunctionList.java` | `FunctionList.kt` | Preserves final random-access construction, JDK `Function` forward/reverse contracts, mapped-list write bridges and return values, exact forward/reverse diagnostics, forward-function identity, ordinary/advanced function switching, advanced reevaluation/disposal arguments and the longstanding source-copy reorder behavior, exact mapped events, source-event batching in `removeIf`, and inherited listener disposal. `AdvancedFunction` remains an ordinary three-abstract-method interface rather than a Kotlin functional interface. The Kotlin `forwardFunction` property is nullable solely so raw Java `setForwardFunction(null)` retains the exact `IllegalArgumentException`; its getter remains non-null at runtime. |
| `impl/FunctionListMap.java` | `impl/FunctionListMap.kt` | Preserves open Java construction, the mutable/disposable map surface, key/value agreement and duplicate diagnostics with their partial-state timing, identity-based replacement/removal for random-access and other sources, two-pass event processing, source-order live views, cached key/entry views and write-through iterators, snapshot entry behavior, object-typed entry membership/removal including read-only Kotlin entries and raw Java unsupported-add failures, null key/value edge cases, map equality/hash/forEach, and disposal detachment. Original public/protected descriptors remain; Kotlin adds one module-mangled JVM-synthetic event-processing helper that is hidden from normal Java source. |
| `impl/GroupingListMultiMap.java` | `impl/GroupingListMultiMap.kt` | Preserves comparator-driven grouping without source reordering, live group identity, validated writable group wrappers and iterators, source insert/delete/key-change synchronization, the longstanding same-key UPDATE-silence partial state, map `put`/`putAll` validation and failure ordering, raw grouping-list `values`, cached write-through key/entry views, object-typed entry membership and raw Java unsupported-add behavior, writable snapshot entries and their add-before-remove replacement algorithm, null-key/value edge cases, JDK-default two-argument removal, and disposal detachment. Public Java construction, `DisposableMap`, `ListEventListener`, map-entry, array, mutable `List`, and runtime JSpecify view annotations remain available. The vendored class now implements the typed Kotlin map contract directly; obsolete external-Java-subclass override compatibility was removed with its temporary Java superclass. |
| `impl/adt/barcode2/BciiNode.java` | — | Removed because the obsolete M4 template node had no callers outside the retired template family. |
| `impl/adt/barcode2/BciiTree.java` | — | Removed because the obsolete M4 template tree had no callers outside the retired template family. |
| `impl/adt/barcode2/BciiTreeIterator.java` | — | Removed because the obsolete M4 template iterator had no callers outside the retired template family. |
| `impl/adt/barcode2/BciiTreeAsList.java` | — | Removed with its unused generated variants; the obsolete M4 macro files and include directives were also removed because the build has no source-generation hook. |
| `impl/adt/barcode2/SimpleTreeAsList.java` | — | Removed because the generated mutable-list adapter had no production or test callers. |
| `impl/adt/barcode2/FourColorTreeAsList.java` | — | Removed because the generated mutable-list adapter had no production or test callers. |
| `impl/adt/CircularArrayList.java` | — | Removed because the unused compatibility collection had no remaining callers. |
| `impl/adt/IntArrayList.java` | — | Removed; primitive event-block storage is owned directly by `BlockSequence.kt`. |
| `impl/adt/KeyedCollection.java` | — | Removed with its unused `GlazedListsImpl` factory overloads because there were no production or test callers. |
| `impl/GlazedListsImpl.java` | `impl/GlazedListsImpl.kt` | Preserves Java-static utility methods, sorted replacement behavior, equality and identity factories, and the non-instantiable utility-class contract. |
| `impl/IteratorAsEnumeration.java` | — | Removed because the unused JDK collection adapter had no remaining callers. |
| `impl/adt/AgedNode.java` | — | Removed because the abandoned age-sorted cache node had no production callers. |
| `impl/adt/AgedNodeComparator.java` | — | Removed with the unused `AgedNode` cache implementation. |
| `impl/ObservableConnector.java` | `impl/ObservableConnector.kt` | Replaces deprecated `Observable`/`Observer` with a property-change contract. |
| `impl/event/BlockSequence.java` | `impl/event/BlockSequence.kt` | Uses reusable `IntArray` storage while preserving block ordering, concatenation, iteration, and Java-facing contracts. |
| `impl/event/Tree4Deltas.java` | `impl/event/Tree4Deltas.kt` | Stores old and new values explicitly, fixes range-value isolation and contradictory insert updates, and removes the unused source-side API. |
| `event/ListEvent.java` | `event/ListEvent.kt` | Preserves the public abstract event contract, protected mutable source-list field, overridable source-list accessor, Java-static constants and generic `unknownValue()` singleton API, old/new/reorder abstract method descriptors, and EventObject-backed null-source failure. The Kotlin port necessarily widens the JVM constructor from package-private to protected because Kotlin cannot emit a package-private constructor on a public class; Kotlin's required companion also adds the public static final `Companion` field and makes the `@JvmStatic unknownValue()` bridge final. Kotlin property conveniences are JVM-synthetic so normal Java source sees the legacy accessor surface. |
| `event/Tree4DeltasListEvent.java` | `event/Tree4DeltasListEvent.kt` | Preserves the package-private JVM class boundary, constructor shape used by `ListEventAssembler.kt`, linear-versus-tree reset behavior, iterator copy independence, reorder-map aliasing/message, remaining-block counts, and exact event string rendering. |
| `swing/GlazedListsSwing.java` | `swing/GlazedListsSwing.kt` | Preserves the Java-static Swing factory facade and source-disposal behavior. |
| `swing/MutableListDataEvent.java` | `swing/MutableListDataEvent.kt` | Preserves reusable event state, Java construction, and debugging output. |
| `swing/MutableTableModelEvent.java` | `swing/MutableTableModelEvent.kt` | Preserves reusable row/type state and Glazed Lists-to-Swing event mapping. |
| `swing/DefaultEventListModel.java` | `swing/DefaultEventListModel.kt` | Preserves reusable event dispatch, protected subclass hooks, source locking, and optional source disposal. |
| `swing/DefaultEventComboBoxModel.java` | `swing/DefaultEventComboBoxModel.kt` | Preserves reference-identity selection changes and Java subclassing. |
| `swing/AdvancedListSelectionModel.java` | `swing/AdvancedListSelectionModel.kt` | Uses Kotlin properties while preserving all JavaBean getter/setter descriptors and the Swing selection-model contract. |
| `swing/DefaultEventSelectionModel.java` | `swing/DefaultEventSelectionModel.kt` | Preserves source locking, adjustment-event aggregation, listener snapshots and event identity, stable mutation views, disabled-state asymmetries, defensive insertion handling, both constructors, and Java selection-model interfaces. |
| `swing/UndoSupport.java` | `swing/UndoSupport.kt` | Preserves EDT enforcement, Java-static installation overloads, custom edit adapters, undo/redo delegation, and uninstall cleanup. |
| `swing/SortableRenderer.java` | `swing/SortableRenderer.kt` | Uses a Kotlin functional interface while preserving the `Consumer` bridge and nullable sort icons. |
| `swing/TableModelEventAdapter.java` | `swing/TableModelEventAdapter.kt` | Preserves the event-adapter and Java-functional factory contracts. |
| `swing/AdvancedTableModel.java` | `swing/AdvancedTableModel.kt` | Exposes the table format as a Kotlin property while preserving Java getter and setter signatures. |
| `swing/DefaultEventTableModel.java` | `swing/DefaultEventTableModel.kt` | Preserves source locking, editable formats, protected subclass hooks, adapter replacement, and optional source disposal. |
| `swing/TableComparatorChooser.java` | `swing/TableComparatorChooser.kt` | Preserves Java-static installation methods, renderer and listener lifecycle, model replacement, sort notifications, and comparator redetection. |
| `impl/swing/LowerThresholdRangeModel.java` | `impl/swing/LowerThresholdRangeModel.kt` | Preserves threshold normalization, source locking, Swing change events, and Java subclassing. |
| `impl/swing/UpperThresholdRangeModel.java` | `impl/swing/UpperThresholdRangeModel.kt` | Preserves threshold normalization, source locking, Swing change events, and Java subclassing. |
| `impl/swing/DefaultTableModelEventAdapter.java` | `impl/swing/TableModelEventAdapters.kt` | Preserves its package-private class boundary, precise block events, and EDT enforcement. |
| `impl/swing/ManyToOneTableModelEventAdapter.java` | `impl/swing/TableModelEventAdapters.kt` | Preserves reorder handling and multi-block collapse behavior. |
| `impl/swing/DefaultTableModelEventAdapterFactory.java` | `impl/swing/TableModelEventAdapters.kt` | Preserves public construction and the generic singleton factory. |
| `impl/swing/ManyToOneTableModelEventAdapterFactory.java` | `impl/swing/TableModelEventAdapters.kt` | Preserves public construction and the generic singleton factory. |
| `impl/beans/BeanConnector.java` | `impl/beans/BeanConnector.kt` | Uses Kotlin reflection-call and null-safety idioms. |
| `impl/beans/BeanFunction.java` | `impl/beans/BeanFunction.kt` | No intentional behavior change. |
| `impl/beans/StringBeanFunction.java` | `impl/beans/StringBeanFunction.kt` | Makes its nullable string result explicit; no intentional behavior change. |
| `impl/beans/BeanThresholdEvaluator.java` | `impl/beans/BeanThresholdEvaluator.kt` | Preserves lazy property discovery and integer evaluation. |
| `impl/beans/BeanTextFilterator.java` | `impl/beans/BeanTextFilterator.kt` | Preserves lazy and eager property discovery and explicit null-element handling. |
| `impl/beans/BeanProperty.java` | `impl/beans/BeanProperty.kt` | Preserves nested reflective access, Java trailing-separator splitting, generic type resolution, diagnostic exceptions, exact-class equality, and Java subclassing. |
| `impl/beans/BeanTableFormat.java` | `impl/beans/BeanTableFormat.kt` | Preserves lazy and eager descriptors, Java null inputs and nullable edited values, primitive wrapper classes and immutable primitive map, protected Java subclass state and hooks, and both constructors. |
| `gui/AbstractTableComparatorChooser.java` | `gui/AbstractTableComparatorChooser.kt` | Preserves subclass extension points, Java-record sort keys, immutable sort-key snapshots, and null-validation messages. |
| `gui/AdvancedTableFormat.java` | `gui/AdvancedTableFormat.kt` | Makes the nullable column comparator explicit; no intentional behavior change. |
| `gui/CheckableTableFormat.java` | `gui/CheckableTableFormat.kt` | No intentional behavior change. |
| `gui/TableFormat.java` | `gui/TableFormat.kt` | Makes nullable cell values explicit; no intentional behavior change. |
| `gui/WritableTableFormat.java` | `gui/WritableTableFormat.kt` | No intentional behavior change. |
| `impl/gui/MouseOnlySortingStrategy.java` | `impl/gui/MouseOnlySortingStrategy.kt` | Preserves single- and multiple-column click behavior. |
| `impl/gui/SortingStrategy.java` | `impl/gui/SortingStrategy.kt` | Preserves the Java functional-interface annotation, generic click method, and default multiple-column capability. |
| `impl/gui/SortingState.java` | `impl/gui/SortingState.kt` | Preserves comparator detection, column precedence, sorting-style codes, mutable Java-facing column state, and the non-static inner-column contract. |
| `impl/SortIconFactory.java` | `impl/SortIconFactory.kt` | Preserves cached default icons, fresh explicit-path arrays, and Java-static loading methods. |
| `impl/SimpleFunctionList.java` | `impl/SimpleFunctionList.kt` | Preserves read-only mapping, forwarded list events, disposal behavior, and the null-function diagnostic. |
| `impl/ListCollectionListModel.java` | `impl/ListCollectionListModel.kt` | Preserves nullable-parent handling, parent identity, Java subclassing, and the exact Java generic model contract. |
| `impl/SimpleIterator.java` | `impl/SimpleIterator.kt` | Preserves Java subclassing, source mutation, cursor behavior, and exact failure diagnostics. |
| `impl/EventListIterator.java` | `impl/EventListIterator.kt` | Preserves all constructors, Java subclassing, bidirectional mutation, cursor adjustment across source changes, and exact failure diagnostics. |
| `impl/SubEventList.java` | `impl/SubEventList.kt` | Preserves writable range views, source-relative index shifts, event translation, single-element reorder tracking, and listener-proxy registration. |
| `impl/ReadOnlyList.java` | `impl/ReadOnlyList.kt` | Preserves live event forwarding, delegated accessors and array snapshots, Java-visible JSpecify type-use annotations, and exact mutation failures. |
| `impl/TypeSafetyListener.java` | `impl/TypeSafetyListener.kt` | Preserves Java subclassing, defensive type snapshots, nullable allowed types, and insert/update diagnostics. |
| `impl/WeakReferenceProxy.java` | `impl/WeakReferenceProxy.kt` | Preserves weak forwarding, Java null validation, self-removal, and post-disposal event suppression. |
| `impl/filter/SearchTerm.java` | `impl/filter/SearchTerm.kt` | Uses a data class while excluding reusable scratch state from value equality. |
| `impl/gui/ThreadProxyEventList.java` | `impl/gui/ThreadProxyEventList.kt` | Adds idempotent disposal and protects queued event state. |
| `impl/swing/SwingThreadProxyEventList.java` | `impl/swing/SwingThreadProxyEventList.kt` | No intentional behavior change. |
| `matchers/ThreadedMatcherEditor.java` | `matchers/ThreadedMatcherEditor.kt` | Uses coroutines and virtual threads and is explicitly closeable. |
| `impl/functions/ConstantFunction.java` | `impl/functions/ConstantFunction.kt` | No intentional behavior change. |
| `impl/filter/StringLengthComparator.java` | `impl/filter/StringLengthComparator.kt` | No intentional behavior change. |
| `impl/filter/StringTextFilterator.java` | `impl/filter/StringTextFilterator.kt` | Makes nullable element handling explicit; no intentional behavior change. |
| `impl/filter/BoyerMooreCaseInsensitiveTextSearchStrategy.java` | `impl/filter/BoyerMooreCaseInsensitiveTextSearchStrategy.kt` | Preserves locale-independent Unicode case folding, UTF-16 result indices, and shortened shift-table behavior. |
| `impl/filter/StartsWithCaseInsensitiveTextSearchStrategy.java` | `impl/filter/StartsWithCaseInsensitiveTextSearchStrategy.kt` | Preserves specialized single- and multi-character prefix matching and default-locale case conversion. |
| `impl/filter/ExactCaseInsensitiveTextSearchStrategy.java` | `impl/filter/ExactCaseInsensitiveTextSearchStrategy.kt` | Preserves the open inheritance and exact-length matching contracts. |
| `impl/sort/BooleanComparator.java` | `impl/sort/BooleanComparator.kt` | Preserves null-first ordering and class-based equality. |
| `impl/sort/ComparableComparator.java` | `impl/sort/ComparableComparator.kt` | Preserves null-first natural ordering and class-based equality. |
| `impl/sort/ReverseComparator.java` | `impl/sort/ReverseComparator.kt` | Rejects a null source comparator at construction. |
| `impl/sort/ComparatorChain.java` | `impl/sort/ComparatorChain.kt` | Preserves both constructors, defensive array copies, and content-based equality; no longer a Java record. |
| `impl/sort/TableColumnComparator.java` | `impl/sort/TableColumnComparator.kt` | Preserves both constructors, exact-class equality, and helpful comparison failures. |
| `impl/matchers/TrueMatcher.java` | `impl/matchers/TrueMatcher.kt` | Preserves the generic singleton factory. |
| `impl/matchers/FalseMatcher.java` | `impl/matchers/FalseMatcher.kt` | Preserves the generic singleton factory. |
| `impl/matchers/NullMatcher.java` | `impl/matchers/NullMatcher.kt` | Preserves the generic singleton factory and string representation. |
| `impl/matchers/NotNullMatcher.java` | `impl/matchers/NotNullMatcher.kt` | Preserves the generic singleton factory and string representation. |
| `impl/matchers/NonNullAndNonEmptyStringMatcher.java` | `impl/matchers/NonNullAndNonEmptyStringMatcher.kt` | Makes the accepted nullable input explicit. |
| `impl/matchers/NotMatcher.java` | `impl/matchers/NotMatcher.kt` | Preserves null-parent validation and string representation. |
| `impl/matchers/TypeMatcher.java` | `impl/matchers/TypeMatcher.kt` | No intentional behavior change. |
| `impl/matchers/AndMatcher.java` | `impl/matchers/AndMatcher.kt` | Preserves contravariant matcher varargs. |
| `impl/matchers/OrMatcher.java` | `impl/matchers/OrMatcher.kt` | Preserves contravariant matcher varargs. |
| `impl/matchers/BeanPropertyMatcher.java` | `impl/matchers/BeanPropertyMatcher.kt` | Makes nullable bean input and expected property values explicit. |
| `impl/matchers/RangeMatcher.java` | `impl/matchers/RangeMatcher.kt` | Preserves inclusive and unbounded ranges, including null comparable behavior. |
| `impl/sort/BeanPropertyComparator.java` | `impl/sort/BeanPropertyComparator.kt` | Makes nullable bean comparison explicit. |
| `impl/filter/AbstractTextSearchStrategy.java` | `impl/filter/AbstractTextSearchStrategy.kt` | Preserves optional character mapping and subclass extension points. |
| `impl/filter/RegularExpressionTextSearchStrategy.java` | `impl/filter/RegularExpressionTextSearchStrategy.kt` | Preserves full-input regular-expression matching and initialization behavior. |
| `impl/filter/SingleCharacterCaseInsensitiveTextSearchStrategy.java` | `impl/filter/SingleCharacterCaseInsensitiveTextSearchStrategy.kt` | Preserves single-character validation, character mapping, and case-insensitive search. |
| `impl/filter/TextMatcher.java` | `impl/filter/TextMatcher.kt` | Preserves normalized search terms, strategy selection, and matcher equality. |
| `impl/filter/TextMatchers.java` | `impl/filter/TextMatchers.kt` | Preserves parsing, normalization, matching, and constraint/relaxation classification. |
| `impl/filter/TextSearchStrategy.java` | `impl/filter/TextSearchStrategy.kt` | Preserves the search-strategy and factory functional-interface contracts. |
| `impl/matchers/PropertyEventNameMatcher.java` | `impl/matchers/PropertyEventNameMatcher.kt` | Preserves vararg and collection construction and include/exclude semantics. |
| `impl/matchers/WeakReferenceMatcherEditor.java` | `impl/matchers/WeakReferenceMatcherEditor.kt` | Preserves weak listener registration, cleanup, and event rebroadcasting. |
| `impl/text/LatinDiacriticsStripper.java` | `impl/text/LatinDiacriticsStripper.kt` | Generates the Latin mapping table once with canonical Unicode decomposition and returns defensive snapshots. |
| `matchers/AbstractMatcherEditor.java` | `matchers/AbstractMatcherEditor.kt` | Preserves matcher state transitions and match-all/match-none identity checks. |
| `matchers/AbstractMatcherEditorListenerSupport.java` | `matchers/AbstractMatcherEditorListenerSupport.kt` | Preserves consistently sourced events and LIFO delivery to a stable listener snapshot. |
| `matchers/CompositeMatcherEditor.java` | `matchers/CompositeMatcherEditor.kt` | Preserves AND/OR composition and matcher-event relationship classification. |
| `matchers/FixedMatcherEditor.java` | `matchers/FixedMatcherEditor.kt` | Preserves the package-internal immutable matcher-editor implementation. |
| `matchers/Matcher.java` | `matchers/Matcher.kt` | Uses a Kotlin functional interface while preserving the `Predicate` bridge. |
| `matchers/MatcherEditor.java` | `matchers/MatcherEditor.kt` | Preserves listener, event, and fixed-editor factory contracts. |
| `matchers/Matchers.java` | `matchers/Matchers.kt` | Preserves the Java-static matcher factory and collection utility facade. |
| `matchers/RangeMatcherEditor.java` | `matchers/RangeMatcherEditor.kt` | Preserves inclusive, unbounded, and normalized ranges and event classification. |
| `matchers/SearchEngineTextMatcherEditor.java` | `matchers/SearchEngineTextMatcherEditor.kt` | Preserves search parsing, named fields, defensive field copies, and the Java-record field contract. |
| `matchers/SetMatcherEditor.java` | `matchers/SetMatcherEditor.kt` | Preserves blacklist/whitelist modes, defensive match-set copies, and event classification. |
| `matchers/TextMatcherEditor.java` | `matchers/TextMatcherEditor.kt` | Preserves matching modes, strategy singletons, and matcher-event classification. |
| `matchers/ThresholdMatcherEditor.java` | `matchers/ThresholdMatcherEditor.kt` | Preserves comparison operations, nullable thresholds, comparator fallback, and event classification. |
| `swing/EventTableColumnModel.java` | `swing/EventTableColumnModel.kt` | Preserves EDT-proxy ownership, source locking, live column enumeration, identity-based property-listener transfer, list-event coordinates and failure ordering, selection movement and post-disposal forwarding, width caching, reusable margin events, protected subclass hooks, and the runtime-null protected source field. Kotlin additionally declares the inherited `ListEventListener`/`Consumer` `accept` bridges. |
| `swing/SearchEngineTextFieldMatcherEditor.java` | `swing/SearchEngineTextFieldMatcherEditor.kt` | Preserves initial and action-triggered filtering, listener disposal, nullable filterators, and Java subclassing. |
| `swing/TextComponentMatcherEditor.java` | `swing/TextComponentMatcherEditor.kt` | Preserves the three constructors and nullable filterator, live and action-only listener state machine, document replacement, Java whitespace splitting, initial refiltering, disposal and partial-failure behavior, and Java subclassability without exposing synthetic state accessors. |
| `swing/AutoCompleteSupport.java` | `swing/AutoCompleteSupport.kt` | Preserves EDT-only installation and mutation, source locking, filtered and first-item views, strict and non-strict completion, case correction, prefix/contains filtering, custom text strategies and formats, document/model/UI listener lifecycle, action suppression and ordering, popup and focus behavior, uninstall disposal, and sorted/distinct live table-cell editor factories. The Java-static factories, public generic signatures, JavaBean accessors, and extensible `AutoCompleteCellEditor` remain source- and binary-compatible. Accepted implementation-only ABI drift comprises the Kotlin companion and synthetic private-constructor marker, Kotlin metadata, and package-private state/helper classes with synthetic implementation accessors; no additional outer or cell-editor API is exposed. |

## Remaining Java migration registry

`RemainingJavaMigrationTest` is the authoritative manifest for the vendored
Glazed Lists Java sources that still need conversion. After the SimpleTree,
FourColorTree, Barcode, SparseList, SequenceDependenciesEventPublisher, ListEvent, Tree4DeltasListEvent, ListEventAssembler, AbstractEventList, Diff, Grouper, CollectionList, FunctionList, FunctionListMap, GroupingListMultiMap, ObservableElementList, ListSelection, DebugList, SeparatorList, GlazedLists, EventTableColumnModel, TextComponentMatcherEditor, and AutoCompleteSupport migrations, `src/main/java/ca/odell/glazedlists` contains no remaining
conversion candidates or intentionally retained Java compatibility boundaries (0 physical production Java files).

See `LICENSE` in this directory for the upstream licensing terms.
